import { expect } from 'chai';
import { describe, it } from 'mocha'

import { TransectionRequest} from '../proto/transection.js'

function testDataInit() {
    let map = new Map<string, TransectionRequest>()
}

describe('priceCalculate', function() {
    for(let i=0;i<10;i++) {
        it(`token`, function() {   
        });
    }
});